<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.padel_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Padel_android"
        android:usesCleartextTraffic="true">
        <activity android:name=".crud.Add_Reserva"></activity>
        <activity android:name=".crud.AgendaDeReservas" />
        <activity android:name=".crud.EmailCRud" />
        <activity android:name=".crud.Register" />
        <activity android:name=".crud.Edit_Reserva" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>